<UserControl x:Class="NintendoLibrary.NintendoLibrarySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:p="clr-namespace:Playnite"
             xmlns:local="clr-namespace:NintendoLibrary"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:NintendoLibrarySettingsHandler}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock FontSize="16" Text="{p:LocalizedString {x:Static p:LocId.nintendo_library_label}}" />
            <CheckBox Margin="0,12,0,0"
                      Content="{p:LocalizedString {x:Static p:LocId.settings_connect_account}}"
                      IsChecked="{Binding Settings.ConnectAccount}" />
            <CheckBox Margin="0,8,0,0"
                      Content="{p:LocalizedString {x:Static p:LocId.nintendo_exclude_addon_only_label}}"
                      IsChecked="{Binding Settings.ExcludeAddOnOnlyEntries}" />
            <StackPanel Margin="0,16,0,0"
                        IsEnabled="{Binding Settings.ConnectAccount}">
                <Button Width="130"
                        HorizontalAlignment="Left"
                        Command="{Binding LoginCommand}"
                        Content="{p:LocalizedString {x:Static p:LocId.authenticate_label}}" />
                <TextBlock Margin="0,8,0,0"
                           Text="{Binding AuthenticationStatus}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
